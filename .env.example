# Copy to .env.local and fill in. Without a key the app runs in demo mode.

# Required for live generation — get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional: override the model (defaults to claude-sonnet-5)
# CREATIVE_MODEL=claude-sonnet-5
